<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Pulsaar.Controls.DeviceHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24,24,24,20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="64"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <FontIcon x:Name="KindIcon"
                  Grid.Column="0"
                  FontSize="44"
                  VerticalAlignment="Center"
                  HorizontalAlignment="Center"
                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

        <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center" Spacing="6">
            <TextBlock x:Name="NameText"
                       Style="{StaticResource TitleTextBlockStyle}"
                       TextTrimming="CharacterEllipsis"/>
            <StackPanel Orientation="Horizontal" Spacing="12">
                <TextBlock x:Name="StatusText"
                           Style="{StaticResource CaptionTextBlockStyle}"/>
                <StackPanel x:Name="BatteryPanel"
                            Orientation="Horizontal"
                            Spacing="4"
                            Visibility="Collapsed">
                    <FontIcon x:Name="BatteryIcon"
                              FontSize="12"
                              VerticalAlignment="Center"/>
                    <TextBlock x:Name="BatteryLevelText"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
